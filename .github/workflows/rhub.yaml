# R-hub's generic GitHub Actions workflow file. It's canonical location is at
# https://github.com/r-hub/actions/blob/v1/workflows/rhub.yaml
# You can update this file to a newer version using the rhub2 package:
#
# rhub::rhub_setup()
#
# It is unlikely that you need to modify this file manually.

name: R-hub
run-name: "${{ github.event.inputs.id }}: ${{ github.event.inputs.name || format('Manually run by {0}', github.triggering_actor) }}"

on:
  workflow_dispatch:
    inputs:
      config:
        description: 'A comma separated list of R-hub platforms to use.'
        type: string
        default: 'linux,windows,macos'
      name:
        description: 'Run name. You can leave this empty now.'
        type: string
      id:
        description: 'Unique ID. You can leave this empty now.'
        type: string

jobs:

  setup:
    runs-on: ubuntu-latest
    outputs:
      containers: ${{ steps.rhub-setup.outputs.containers }}
      platforms: ${{ steps.rhub-setup.outputs.platforms }}

    steps:
    # NO NEED TO CHECKOUT HERE
    - uses: r-hub/actions/setup@v1
      with:
        config: ${{ github.event.inputs.config }}
      id: rhub-setup

  linux-containers:
    needs: setup
    if: ${{ needs.setup.outputs.containers != '[]' }}
    runs-on: ubuntu-latest
    name: ${{ matrix.config.label }}
    strategy:
      fail-fast: false
      matrix:
        config: ${{ fromJson(needs.setup.outputs.containers) }}
    container:
      image: ${{ matrix.config.container }}

    steps:
      - uses: r-hub/actions/checkout@v1

      # Workaround for: AddressSanitizer ODR-violation between libtbb.so and libtbbmalloc.so
      # (Intel TBB / RcppParallel). ASan hint: ASAN_OPTIONS=detect_odr_violation=0
      # Must be set before setup-deps so loading R during pak install does not abort.
      - name: ASan suppress TBB ODR false positive (clang-asan / gcc-asan)
        if: ${{ matrix.config.label == 'clang-asan' || matrix.config.label == 'gcc-asan' }}
        run: echo "ASAN_OPTIONS=detect_odr_violation=0" >> "$GITHUB_ENV"

      - uses: r-hub/actions/platform-info@v1
        with:
          token: ${{ secrets.RHUB_TOKEN }}
          job-config: ${{ matrix.config.job-config }}
      - uses: r-hub/actions/setup-deps@v1
        with:
          token: ${{ secrets.RHUB_TOKEN }}
          job-config: ${{ matrix.config.job-config }}

      # Workaround for: lazy-load fails loading RcppArmadillo.so — libquadmath.so.0 not found.
      # Intel platform uses a Fedora container; RcppArmadillo (via gfortran/quadmath) needs the
      # libquadmath runtime package on the image. Not a glmbayes R code issue.
      - name: Install libquadmath (Fedora / Intel container)
        if: ${{ matrix.config.label == 'intel' }}
        run: |
          set -eux
          if command -v dnf >/dev/null 2>&1; then
            dnf install -y libquadmath
          elif command -v microdnf >/dev/null 2>&1; then
            microdnf install -y libquadmath
          else
            echo "Expected dnf or microdnf on this image" >&2
            exit 1
          fi

      # Workaround for: R_NamespaceRegistry in Rcpp/Function.h (R-devel vs Rcpp headers).
      # CRAN source Rcpp can still lag R-devel; GitHub Rcpp matches (same idea as Windows job).
      - name: Diagnostics (R / Rcpp)
        if: ${{ matrix.config.label == 'valgrind' || matrix.config.label == 'nosuggests' || matrix.config.label == 'clang-asan' || matrix.config.label == 'gcc-asan' }}
        run: |
          Rscript -e "cat('R:', R.version.string, '\n')"
          Rscript -e "if (requireNamespace('Rcpp', quietly=TRUE)) cat('Rcpp:', as.character(packageVersion('Rcpp')), '\n') else cat('Rcpp: <not installed>\\n')"

      # nosuggests: setup-deps does not install Suggests — prepend one lib via R_LIBS so run-check
      # sees rmarkdown/knitr. Do NOT set R_LIBS_USER to that path: it replaces the default user
      # library and hides packages setup-deps installed (e.g. rcmdcheck for run-check).
      - name: Nosuggests — library path for Rcpp + vignette toolchain
        if: ${{ matrix.config.label == 'nosuggests' }}
        run: |
          set -eux
          CLEAN="${RUNNER_TEMP:-/tmp}/rhub-nosuggests-lib"
          mkdir -p "$CLEAN"
          echo "R_LIBS=$CLEAN" >> "$GITHUB_ENV"

      - name: Install Rcpp from CRAN (source) for R-devel (nosuggests)
        if: ${{ matrix.config.label == 'nosuggests' }}
        run: |
          Rscript -e 'lib <- Sys.getenv("R_LIBS"); stopifnot(nzchar(lib)); install.packages("Rcpp", lib = lib, repos = "https://cloud.r-project.org", type = "source")'

      # valgrind + ASan: R-devel needs headers from GitHub when CRAN Rcpp lags (R_NamespaceRegistry).
      # clang-asan / gcc-asan: R built with ASan+LSan can exit 1 on trivial R-internal leaks during install
      # (e.g. connections.c); LSAN_OPTIONS=exitcode=0 avoids failing remotes::install_github only.
      - name: Install Rcpp from GitHub (valgrind / clang-asan / gcc-asan)
        if: ${{ matrix.config.label == 'valgrind' || matrix.config.label == 'clang-asan' || matrix.config.label == 'gcc-asan' }}
        env:
          GITHUB_TOKEN: ${{ github.token }}
          LSAN_OPTIONS: exitcode=0
        run: |
          Rscript -e "install.packages('remotes', repos='https://cloud.r-project.org')"
          Rscript -e 'lib <- .libPaths()[1L]; try(remove.packages("Rcpp", lib = lib), silent = TRUE); remotes::install_github("RcppCore/Rcpp", upgrade = "never", auth_token = Sys.getenv("GITHUB_TOKEN"))'

      # After GitHub Rcpp: lazy-load fails on RcppArmadillo.so — libflexiblas.so.3 (CRAN binary links FlexiBLAS).
      # Rebuild RcppArmadillo from source against R's BLAS (no FlexiBLAS runtime on image).
      - name: Rebuild RcppArmadillo from source (valgrind)
        if: ${{ matrix.config.label == 'valgrind' }}
        run: |
          Rscript -e "lib <- .libPaths()[1L]; if ('RcppArmadillo' %in% rownames(installed.packages(lib.loc=lib, noCache=TRUE))) remove.packages('RcppArmadillo', lib=lib); install.packages('RcppArmadillo', lib=lib, repos='https://cloud.r-project.org', type='source', Ncpus=2L)"

      # nosuggests omits Suggests, but vignette re-build still needs VignetteBuilder packages (not glmbayes code).
      - name: Install vignette toolchain (nosuggests)
        if: ${{ matrix.config.label == 'nosuggests' }}
        run: |
          Rscript -e 'lib <- Sys.getenv("R_LIBS"); stopifnot(nzchar(lib)); install.packages(c("knitr", "rmarkdown"), lib = lib, repos = "https://cloud.r-project.org", dependencies = TRUE); cat("rmarkdown:", system.file(package = "rmarkdown"), "\n")'
      - uses: r-hub/actions/run-check@v1
        with:
          token: ${{ secrets.RHUB_TOKEN }}
          job-config: ${{ matrix.config.job-config }}

      # On install ERROR, R points to check/*/00install.out — upload so you can download from the Actions UI.
      - name: Upload R CMD check logs (on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: rhub-check-${{ matrix.config.label }}
          if-no-files-found: warn
          path: |
            check/**/00install.out
            check/**/00check.log

  other-platforms:
    needs: setup
    if: ${{ needs.setup.outputs.platforms != '[]' }}
    runs-on: ${{ matrix.config.os }}
    name: ${{ matrix.config.label }}
    strategy:
      fail-fast: false
      matrix:
        config: ${{ fromJson(needs.setup.outputs.platforms) }}

    steps:
      - uses: r-hub/actions/checkout@v1
      - uses: r-hub/actions/setup-r@v1
        with:
          job-config: ${{ matrix.config.job-config }}
          token: ${{ secrets.RHUB_TOKEN }}
      - uses: r-hub/actions/platform-info@v1
        with:
          token: ${{ secrets.RHUB_TOKEN }}
          job-config: ${{ matrix.config.job-config }}
      - uses: r-hub/actions/setup-deps@v1
        with:
          job-config: ${{ matrix.config.job-config }}
          token: ${{ secrets.RHUB_TOKEN }}

      # Workaround for: R_NamespaceRegistry in Rcpp/Function.h on Windows R-devel (CRAN Rcpp can lag R-devel).
      # Prepend a clean R_LIBS so GitHub Rcpp shadows any stale install on another path (common CI footgun).
      - name: Diagnostics (R / Rcpp)
        if: ${{ matrix.config.label == 'windows' }}
        shell: bash
        run: |
          Rscript -e "cat('R:', R.version.string, '\n')"
          Rscript -e "if (requireNamespace('Rcpp', quietly=TRUE)) cat('Rcpp:', as.character(packageVersion('Rcpp')), '\n') else cat('Rcpp: <not installed>\\n')"

      - name: Install Rcpp from GitHub (Windows R-devel)
        if: ${{ matrix.config.label == 'windows' }}
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          set -eux
          CLEAN="${RUNNER_TEMP}/rhub-windows-Rcpp"
          mkdir -p "$CLEAN"
          export R_LIBS="$CLEAN"
          echo "R_LIBS=$CLEAN" >> "$GITHUB_ENV"
          Rscript -e "install.packages('remotes', lib = Sys.getenv('R_LIBS'), repos='https://cloud.r-project.org')"
          Rscript -e 'libs <- .libPaths(); tok <- Sys.getenv("GITHUB_TOKEN"); for (L in libs) { ip <- tryCatch(rownames(installed.packages(lib.loc = L, noCache = TRUE)), error = function(e) character()); if ("Rcpp" %in% ip) try(remove.packages("Rcpp", lib = L), silent = TRUE) }; remotes::install_github("RcppCore/Rcpp", lib = Sys.getenv("R_LIBS"), upgrade = "never", auth_token = tok, force = TRUE)'
          Rscript -e 'p <- system.file("include/Rcpp/Function.h", package = "Rcpp"); cat("Rcpp Function.h:", p, "\n"); if (nzchar(p)) cat(any(grepl("R_NamespaceRegistry", readLines(p, warn = FALSE))), "\n")'

      # Workaround for: ubuntu-next — undefined symbol SETLENGTH (stale rlang.so from another lib on path).
      # Prepend a clean library via R_LIBS for this job + run-check; r-lib/rlang from GitHub; rest from source.
      - name: Force rebuild vignette toolchain from source (ubuntu-next)
        if: ${{ matrix.config.label == 'ubuntu-next' }}
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          set -eux
          CLEAN="${RUNNER_TEMP}/rhub-ubuntu-next-lib"
          mkdir -p "$CLEAN"
          export R_LIBS="$CLEAN"
          echo "R_LIBS=$CLEAN" >> "$GITHUB_ENV"
          Rscript -e "cat('R:', R.version.string, '\n'); message('Clean lib: ', Sys.getenv('R_LIBS'))"
          Rscript -e "install.packages('remotes', lib = Sys.getenv('R_LIBS'), repos='https://cloud.r-project.org')"
          Rscript -e 'tok <- Sys.getenv("GITHUB_TOKEN"); lib <- Sys.getenv("R_LIBS"); pkgs <- c("rmarkdown","knitr","yaml","xfun","lifecycle","vctrs","glue","cli","rlang"); for (L in .libPaths()) { ip <- tryCatch(rownames(installed.packages(lib.loc = L, noCache = TRUE)), error = function(e) character()); for (p in intersect(pkgs, ip)) try(remove.packages(p, lib = L), silent = TRUE) }; remotes::install_github(c("r-lib/rlang","r-lib/cli"), lib = lib, upgrade = "never", auth_token = tok, force = TRUE); pkgs_rest <- setdiff(pkgs, c("rlang","cli")); install.packages(rev(pkgs_rest), lib = lib, repos = "https://cloud.r-project.org", type = "source", Ncpus = 2L)'
          Rscript -e 'p <- system.file("libs/rlang.so", package = "rlang"); cat("rlang.so:", p, "\n")'

      # run-check must see the same R_LIBS prepend as the install steps. Only R_LIBS (not R_LIBS_USER):
      # overriding R_LIBS_USER would hide the default user lib where setup-deps put rcmdcheck etc.
      - uses: r-hub/actions/run-check@v1
        if: ${{ matrix.config.label == 'windows' || matrix.config.label == 'ubuntu-next' }}
        env:
          R_LIBS: ${{ matrix.config.label == 'windows' && format('{0}/rhub-windows-Rcpp', runner.temp) || format('{0}/rhub-ubuntu-next-lib', runner.temp) }}
        with:
          job-config: ${{ matrix.config.job-config }}
          token: ${{ secrets.RHUB_TOKEN }}

      - uses: r-hub/actions/run-check@v1
        if: ${{ matrix.config.label != 'windows' && matrix.config.label != 'ubuntu-next' }}
        with:
          job-config: ${{ matrix.config.job-config }}
          token: ${{ secrets.RHUB_TOKEN }}

      # On install ERROR, R points to check/*/00install.out — upload so you can download from the Actions UI.
      - name: Upload R CMD check logs (on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: rhub-check-${{ matrix.config.label }}
          if-no-files-found: warn
          path: |
            check/**/00install.out
            check/**/00check.log
