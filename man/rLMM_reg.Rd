% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rLMM_reg.R
\name{rLMM_reg}
\alias{rLMM_reg}
\alias{rLMMNormal_reg}
\alias{rLMMNormal_reg_known_vcov}
\alias{rLMMNormal_reg_estimated_vcov}
\alias{rLMMindepNormalGamma_reg}
\alias{rLMMindepNormalGamma_reg_known_vcov}
\alias{rLMMindepNormalGamma_reg_estimated_vcov}
\title{Matrix-level replicate-chain Gibbs engines for Bayesian LMMs}
\usage{
rLMMNormal_reg(
  n,
  y,
  x,
  block,
  x_hyper,
  P,
  prior_list,
  pfamily_list,
  start = NULL,
  icm_tol = 1e-10,
  icm_maxit = 200L,
  m_convergence = NULL,
  tv_tol = 0.01,
  re_coef_names = colnames(x),
  group_levels = levels(block),
  group_name = NULL,
  progbar = TRUE,
  verbose = FALSE
)

rLMMNormal_reg_known_vcov(
  n,
  y,
  x,
  block,
  x_hyper,
  P,
  prior_list,
  pfamily_list,
  start = NULL,
  icm_tol = 1e-10,
  icm_maxit = 200L,
  m_convergence = NULL,
  tv_tol = 0.01,
  re_coef_names = colnames(x),
  group_levels = levels(block),
  group_name = NULL,
  progbar = TRUE,
  verbose = FALSE
)

rLMMNormal_reg_estimated_vcov(
  n,
  y,
  x,
  block,
  x_hyper,
  P,
  prior_list,
  pfamily_list,
  start = NULL,
  icm_tol = 1e-10,
  icm_maxit = 200L,
  m_convergence = NULL,
  tv_tol = 0.01,
  re_coef_names = colnames(x),
  group_levels = levels(block),
  group_name = NULL,
  progbar = TRUE,
  verbose = FALSE,
  gap_tol = 0.0196,
  mode_gap_max = 1,
  diag_sweeps = FALSE,
  stage_verbose = FALSE
)

rLMMindepNormalGamma_reg(
  n,
  y,
  x,
  block,
  x_hyper,
  P,
  prior_list,
  pfamily_list,
  dispersion_fix,
  start = NULL,
  icm_tol = 1e-10,
  icm_maxit = 200L,
  m_convergence = NULL,
  tv_tol = 0.01,
  re_coef_names = colnames(x),
  group_levels = levels(block),
  group_name = NULL,
  progbar = TRUE,
  verbose = FALSE
)

rLMMindepNormalGamma_reg_known_vcov(
  n,
  y,
  x,
  block,
  x_hyper,
  P,
  prior_list,
  pfamily_list,
  dispersion_fix,
  start = NULL,
  icm_tol = 1e-10,
  icm_maxit = 200L,
  m_convergence = NULL,
  tv_tol = 0.01,
  re_coef_names = colnames(x),
  group_levels = levels(block),
  group_name = NULL,
  progbar = TRUE,
  verbose = FALSE
)

rLMMindepNormalGamma_reg_estimated_vcov(
  n,
  y,
  x,
  block,
  x_hyper,
  P,
  prior_list,
  pfamily_list,
  dispersion_fix,
  start = NULL,
  icm_tol = 1e-10,
  icm_maxit = 200L,
  m_convergence = NULL,
  tv_tol = 0.01,
  re_coef_names = colnames(x),
  group_levels = levels(block),
  group_name = NULL,
  progbar = TRUE,
  verbose = FALSE,
  gap_tol = 0.0196,
  mode_gap_max = 1,
  diag_sweeps = FALSE,
  stage_verbose = FALSE
)
}
\arguments{
\item{n}{Number of stored draws. If \code{length(n) > 1}, the length is used.}

\item{y}{Response vector of length \code{nrow(x)}.}

\item{x}{Level-1 design matrix \code{Z} (\code{l2 x p_re}).}

\item{block}{Grouping factor or block partition of length \code{l2}.}

\item{x_hyper}{Named list of group-level design matrices (\code{J x q_k}),
one per column of \code{x}.}

\item{P}{Random-effect prior precision matrix (\code{p_re x p_re}).}

\item{prior_list}{Block~1 prior: \code{list(dispersion = sigma2)} for fixed
\eqn{\sigma^2} routes, \code{dGamma()} fields for legacy
\code{rLMMindepNormalGamma_reg}, or shared ING measurement prior
(\code{mu}, \code{Sigma}, \code{shape}, \code{rate}, \ldots) for ING routes.}

\item{pfamily_list}{Named list of Block~2 \code{pfamily} objects.}

\item{start}{Named list of Block~2 coefficient vectors (ICM start when
\code{NULL}). Computed via \code{\link{lmerb_posterior_mean}} when omitted.}

\item{icm_tol, icm_maxit}{ICM convergence controls when \code{start = NULL}.}

\item{m_convergence}{Inner Gibbs sweeps per stored draw; \code{NULL} derives
from Theorem~3 at \code{start} using \code{tv_tol}.}

\item{tv_tol}{Total-variation tolerance in \code{(0, 1)} for calibration.}

\item{re_coef_names}{Names for columns of \code{x}.}

\item{group_levels}{Character vector defining row order of Block~1 draws.}

\item{group_name}{Name for the grouping column in \code{coefficients}.}

\item{progbar}{Show a text progress bar during sampling.}

\item{verbose}{Print convergence calibration / ICM lines.}

\item{gap_tol, mode_gap_max, diag_sweeps, stage_verbose}{Pilot-stage controls for
\code{rLMMNormal_reg_estimated_vcov} and ING estimated routes (see route docs).}

\item{dispersion_fix}{Plug-in \eqn{\sigma^2} for ICM start and TV calibration
when \code{start = NULL} (ING / dGamma routes).}
}
\description{
Gaussian two-block LMM samplers at matrix level (\code{y}, \code{Z},
\code{x_hyper}, \code{pfamily_list}). Each stored draw runs
\code{m_convergence} inner Gibbs sweeps (Block~1 random effects, then
Block~2 hyperparameters). There is no standalone \code{rLMM()} function;
use \code{\link{rGLMM}} for formula-level GLMMs or these matrix engines
via \code{\link{rlmerb}}.
}
\section{Functions}{
\itemize{
\item \code{rLMMNormal_reg()}: Dispatcher for fixed \eqn{\sigma^2}: routes to
\code{\link{rLMMNormal_reg_known_vcov}} or
\code{\link{rLMMNormal_reg_estimated_vcov}} by Block~2 \code{pfamily_list}.

\item \code{rLMMNormal_reg_known_vcov()}: Fixed \eqn{\sigma^2}; all Block~2 \code{dNormal} (known
\eqn{\tau^2_k}). Exact Theorem~3 rate calibration.

\item \code{rLMMNormal_reg_estimated_vcov()}: Fixed \eqn{\sigma^2}; ING Block~2 (estimated \eqn{\tau^2_k}).
Optional pilot stage; conservative \code{disp_lower} rate bound.

\item \code{rLMMindepNormalGamma_reg()}: Legacy outer-loop engine: draws \eqn{\sigma^2} via
\code{\link{rGamma_reg}}, then calls \code{\link{rLMMNormal_reg}} each
replicate. Prefer \code{\link{rLMMindepNormalGamma_reg_known_vcov}} or
\code{\link{rLMMindepNormalGamma_reg_estimated_vcov}} for the ING Block~1
sweep engine used by \code{\link{rlmerb}}.

\item \code{rLMMindepNormalGamma_reg_known_vcov()}: Random \eqn{\sigma^2} (per-group ING Block~1); all
Block~2 \code{dNormal}. Used by \code{\link{rlmerb}} when
\code{dispersion_ranef} is dGamma and Block~2 is all \code{dNormal}.

\item \code{rLMMindepNormalGamma_reg_estimated_vcov()}: Random \eqn{\sigma^2} (per-group ING Block~1); ING Block~2.
Pilot/UB calibration via sweep-outer engine. Default path for
\code{\link{rlmerb}} with dGamma \code{dispersion_ranef} and ING Block~2.

}}
\section{Four route engines}{

\describe{
\item{\code{rLMMNormal_reg_known_vcov}}{
Fixed observation \eqn{\sigma^2}; all Block~2 components \code{dNormal}
(known \eqn{\tau^2_k}).}
\item{\code{rLMMNormal_reg_estimated_vcov}}{
Fixed \eqn{\sigma^2}; at least one ING Block~2 component.}
\item{\code{rLMMindepNormalGamma_reg_known_vcov}}{
Random \eqn{\sigma^2} (per-group ING Block~1); all Block~2 \code{dNormal}.}
\item{\code{rLMMindepNormalGamma_reg_estimated_vcov}}{
Random \eqn{\sigma^2}; at least one ING Block~2 component.}
}
}

\section{Dispatchers}{

\code{\link{rLMMNormal_reg}} and \code{\link{rLMMindepNormalGamma_reg}}
validate inputs and delegate to the appropriate route (or, for the legacy
outer-loop \code{rLMMindepNormalGamma_reg}, draw \eqn{\sigma^2} then call
\code{rLMMNormal_reg}).
}

\seealso{
\code{\link{rGLMM}}, \code{\link{rlmerb}}, \code{\link{rindepNormalGamma_reg}}

glmbayes Simulation Functions
\code{\link{multi_rNormal_reg}()},
\code{\link{multi_rlmb}()},
\code{\link{rGLMM}()},
\code{\link{rGLMM_Re_Draw}()},
\code{\link{rGLMM_sweep}()},
\code{\link{simfuncs}},
\code{\link{two_block_optimize_pilot_cost}()},
\code{\link{two_block_rNormal_reg}()},
\code{\link{two_block_rate}()},
\code{\link{two_block_tv_bound}()}
}
\concept{simfuncs}
