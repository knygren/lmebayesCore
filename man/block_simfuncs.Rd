% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rNormalGLM_reg_block_update.R,
%   R/simfunction_block.R
\name{block_simfuncs}
\alias{block_simfuncs}
\alias{block_rNormalGLM_update}
\alias{rNormalGLM_reg_block_update}
\alias{block_rNormalGLM}
\alias{rNormalGLM_reg_block}
\title{One Gibbs block update via \code{block_rNormalGLM}}
\usage{
block_rNormalGLM_update(
  mu_all,
  sigma_theta_sq = NULL,
  y,
  x,
  block,
  family = poisson(),
  prior_list = NULL,
  prior_lists = NULL,
  offset = NULL,
  weights = 1,
  Gridtype = 2L,
  n_envopt = 1L,
  use_parallel = TRUE,
  use_opencl = FALSE,
  verbose = FALSE,
  progbar = FALSE,
  seed = NULL,
  theta_coef_col = 1L
)

rNormalGLM_reg_block_update(
  mu_all,
  sigma_theta_sq = NULL,
  y,
  x,
  block,
  family = poisson(),
  prior_list = NULL,
  prior_lists = NULL,
  offset = NULL,
  weights = 1,
  Gridtype = 2L,
  n_envopt = 1L,
  use_parallel = TRUE,
  use_opencl = FALSE,
  verbose = FALSE,
  progbar = FALSE,
  seed = NULL,
  theta_coef_col = 1L
)

block_rNormalGLM(
  n,
  y,
  x,
  block,
  prior_list = NULL,
  prior_lists = NULL,
  offset = NULL,
  weights = 1,
  family = gaussian(),
  Gridtype = 2L,
  n_envopt = NULL,
  use_parallel = TRUE,
  use_opencl = FALSE,
  verbose = FALSE,
  progbar = FALSE
)

rNormalGLM_reg_block(
  n,
  y,
  x,
  block,
  prior_list = NULL,
  prior_lists = NULL,
  offset = NULL,
  weights = 1,
  family = gaussian(),
  Gridtype = 2L,
  n_envopt = NULL,
  use_parallel = TRUE,
  use_opencl = FALSE,
  verbose = FALSE,
  progbar = FALSE
)
}
\arguments{
\item{mu_all}{Length-\code{k} vector of prior means per block (linear predictors
from the population block). Required unless \code{prior_lists} or
\code{prior_list} is supplied.}

\item{sigma_theta_sq}{Shared prior variance for scalar blocks when building
\code{prior_lists} from \code{mu_all}.}

\item{y, x, block, family}{Passed to \code{\link{block_rNormalGLM}}.}

\item{x}{Design matrix \code{nrow(x)} by \code{ncol(x)}; same \code{ncol} in every block.}

\item{block}{Block partition: \code{factor}/integer length \code{l2}, \code{l2_blocks}
counts summing to \code{l2}, or list of row index vectors.}

\item{family}{GLM \code{\link{family}} (not \code{gaussian()}); passed to \code{\link[=rNormal_reg]{rNormal_reg()}}.}

\item{prior_list, prior_lists}{Optional; passed through when supplied.}

\item{prior_lists}{Optional list of length \code{k} (or \code{1}) of per-block
\code{prior_list} objects.}

\item{offset, weights}{Passed to \code{\link{block_rNormalGLM}}.}

\item{weights}{Optional weights; same recycling and blocking as \code{offset}.}

\item{Gridtype, use_parallel, use_opencl, verbose, progbar}{Passed to each block's \code{\link[=rNormal_reg]{rNormal_reg()}}.}

\item{n_envopt}{Passed to each block; defaults to \code{1} when \code{NULL}.}

\item{seed}{Optional; passed to \code{\link{set.seed}} before sampling.}

\item{theta_coef_col}{Column index of \code{coefficients} to return as
\code{theta} (default \code{1} for scalar intercept blocks).}

\item{n}{Number of iid draws per block (\code{n = 1} typical for Gibbs).}

\item{y}{Response vector of length \code{nrow(x)}.}

\item{prior_list}{Single prior specification recycled to all blocks, or with
\code{mu} as \code{l1} by \code{k} matrix or \code{blocks} sublist.}

\item{offset}{Optional numeric vector (length \code{1} or \code{length(y)});
partitioned across blocks like \code{y}.}
}
\value{
A list with:
\describe{
\item{theta}{Vector of length \code{nrow(x)} (one draw per row/block).}
\item{coefficients,coef.mode}{Matrices from \code{block_rNormalGLM}.}
\item{block_rNormalGLM}{Full block sampler output.}
}

A list with class \code{"block_rNormalGLM"} including:
\describe{
\item{coefficients}{Matrix \code{k * p}; row \code{b} is the draw for block \code{b}.}
\item{coef.mode}{Matrix \code{k * p}; posterior mode per block.}
\item{block_info}{Block partition metadata.}
\item{block_results}{List of length \code{k} with each block's \code{\link[=rNormal_reg]{rNormal_reg()}} output.}
}
}
\description{
Draw a single blockwise GLM posterior sample (\code{n = 1}) and return
latent-level draws (e.g. updated \code{theta}) for two-block Gibbs samplers
where each observation or group is its own block with scalar (or vector)
coefficients per block.

Draw from blockwise full conditionals when the posterior factorizes across
observation blocks, via \code{.rNormalGLMBlocks_cpp()} (each block calls
\code{rNormalGLM}). Typical use is \strong{block Gibbs} (\code{n = 1} per outer step);
\code{n > 1} gives iid draws from the product conditional.
}
\details{
This is a thin wrapper around \code{\link{block_rNormalGLM}} that always
uses \code{n = 1}. When \code{prior_lists} and \code{prior_list} are both
omitted, per-block \code{dNormal} priors are built from \code{mu_all} and
\code{sigma_theta_sq} (scalar intercept per block).

\strong{BY-style separate fits} (SAS \code{BY}) use \code{\link{block_lmb}} or
\code{\link{block_glmb}}, not this function. \strong{Output layout:} \code{coefficients}
and \code{coef.mode} are matrices with \strong{rows = blocks} and \strong{columns = predictors}.

See \code{inst/DESIGN_RGLM_BLOCKS.md}.
}
\examples{
\dontrun{
if (requireNamespace("glmbayes", quietly = TRUE)) {
  n <- 5L
  y <- rpois(n, 2)
  x <- matrix(1, n, 1)
  mu <- rep(0, n)
  block_rNormalGLM_update(
    mu_all = mu, sigma_theta_sq = 1,
    y = y, x = x, block = seq_len(n),
    family = poisson(), use_parallel = FALSE
  )
}
}
}
\seealso{
\code{\link{block_rNormalGLM}}, \code{\link{block_lmb}}

\link{rNormal_reg}, \link{simfuncs}, \code{inst/DESIGN_RGLM_BLOCKS.md}
}
\concept{block_simfuncs}
