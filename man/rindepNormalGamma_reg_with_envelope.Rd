% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rindepNormalGamma_reg_with_envelope.R
\name{rindepNormalGamma_reg_with_envelope}
\alias{rindepNormalGamma_reg_with_envelope}
\title{Independent Normal--Gamma regression with envelope artifacts returned}
\usage{
rindepNormalGamma_reg_with_envelope(
  n,
  y,
  x,
  prior_list,
  offset = NULL,
  weights = 1,
  family = gaussian(),
  Gridtype = 2,
  n_envopt = NULL,
  use_parallel = TRUE,
  use_opencl = FALSE,
  verbose = FALSE,
  progbar = TRUE
)
}
\arguments{
\item{n}{Number of draws to generate. If \code{length(n) > 1}, the length is taken to be the number required.}

\item{y}{A vector of observations of length \code{m}.}

\item{x}{A design matrix of dimension \code{m * p}.}

\item{prior_list}{A list with prior parameters (e.g., shape, rate, beta) used in the simulation.}

\item{offset}{Optional numeric vector of length \code{m} specifying known components of the linear predictor.}

\item{weights}{Optional numeric vector of prior weights.}

\item{family}{A description of the error distribution and link function (see \code{\link{family}}).}

\item{Gridtype}{Optional integer specifying the method used to construct the envelope function.}

\item{n_envopt}{Effective sample size passed to \code{EnvelopeOpt} for grid
construction. Defaults to match \code{n}. Larger values encourage tighter
envelopes.}

\item{use_parallel}{Logical. Whether to use parallel processing.}

\item{use_opencl}{Logical. Whether to use OpenCL acceleration.}

\item{verbose}{Logical. Whether to print progress messages.}

\item{progbar}{Logical. Whether to display a progress bar during simulation.}
}
\value{
An \code{rglmb} object like \code{glmbayesCore::rindepNormalGamma_reg()},
plus \code{Envelope}, \code{gamma_list}, \code{UB_list}, and \code{diagnostics}.
}
\description{
Diagnostic wrapper with the same sampling pipeline as
\code{glmbayesCore::rindepNormalGamma_reg()}, but also returns the
standardized envelope (\code{Envelope}), \code{gamma_list}, \code{UB_list},
and \code{diagnostics} used by the joint accept--reject sampler
(standardized subproblem; see \code{glmbayesCore::EnvelopeOrchestrator()}).
Intended for parity checks and development; production callers should use
\code{glmbayesCore::rindepNormalGamma_reg()}.
}
