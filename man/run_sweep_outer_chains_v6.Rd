% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_sweep_outer_chains_v6.R
\name{run_sweep_outer_chains_v6}
\alias{run_sweep_outer_chains_v6}
\title{Run independent short Gibbs chains via sweep-outer R driver}
\usage{
run_sweep_outer_chains_v6(
  n_chains,
  start_fixef,
  inner_sweeps,
  design,
  block1_prior,
  pfamily_list,
  family,
  re_names,
  group_levels,
  collect_block1 = TRUE,
  progbar = FALSE,
  stage_label = "",
  diag_sweeps = FALSE,
  fixef_mode = NULL,
  b_mode = NULL,
  b_start = NULL,
  ptypes = NULL
)
}
\arguments{
\item{n_chains}{Integer. Number of independent chains to run.}

\item{start_fixef}{Named list of starting hyper-parameter vectors, one
named numeric vector per RE component (matches \code{re_names}).}

\item{inner_sweeps}{Integer. Number of inner Gibbs sweeps per chain.}

\item{design}{List with components \code{y}, \code{Z}, \code{groups},
\code{X_hyper}, \code{re_coef_names}, and optional \code{group_name}.}

\item{block1_prior}{Block~1 prior list (\code{P} or \code{Sigma},
\code{dispersion}, optional \code{ddef}).}

\item{pfamily_list}{Named list of \code{\link{pfamily}} objects (one per
RE component).}

\item{family}{A \code{\link[stats]{family}} object for the response model.}

\item{re_names}{Character vector of RE coefficient names.}

\item{group_levels}{Character vector of group levels.}

\item{collect_block1}{Logical. If \code{TRUE}, collect and rbind Block~1
(\code{coefficients}) draws from every chain. Default \code{TRUE}.}

\item{progbar}{Logical. When \code{TRUE} (or when \code{diag_sweeps} is
\code{TRUE}), show text progress bars over chains during each inner sweep.
Default \code{FALSE}.}

\item{stage_label}{Character label stored on \code{$sweep_history} (e.g.
\code{"pilot"} or \code{"main"}).}

\item{diag_sweeps}{Legacy flag: when \code{TRUE}, enables progress bars as
for \code{progbar = TRUE}. Sweep history is always collected; use
\code{print()} on \code{$sweep_history} to display tables.}

\item{fixef_mode}{ICM mode reference stored on \code{$sweep_history}.}

\item{b_mode}{ICM mode reference for random-effect diagnostics.}

\item{b_start}{Initial random-effect matrix for all chains (\code{J x p_re}).
Defaults to \code{b_mode} when \code{NULL}.}

\item{ptypes}{Per-component pfamily names (optional; derived from
\code{pfamily_list} when \code{NULL}).}
}
\value{
A list with components \code{fixef_draws}, \code{dispersion_fixef_draws},
\code{iters_fixef_draws}, \code{iters_ranef_draws}, \code{coefficients},
\code{mu_all_last}, and \code{sweep_history} (class
\code{"two_block_sweep_history"}).
}
\description{
Executes \code{n_chains} independent two-block Gibbs chains, each
initialised at \code{start_fixef} and run for \code{inner_sweeps} sweeps.
Loop order is sweep-outer: for each inner sweep, all chains run Block~1,
then all chains run Block~2. Each chain stores exactly one draw
(\code{n = 1L} per chain), so the result is a collection of
\code{n_chains} approximately independent draws from the target posterior
(for large enough \code{inner_sweeps}).
}
\seealso{
\code{\link{two_block_rNormal_reg_v2}}, \code{\link{rGLMM}}

glmbayes Simulation Functions
\code{\link{multi_rNormal_reg}()},
\code{\link{multi_rlmb}()},
\code{\link{rGLMM}()},
\code{\link{rLMMNormal_reg}()},
\code{\link{rLMMNormal_reg_estimated_vcov}()},
\code{\link{rLMMNormal_reg_known_vcov}()},
\code{\link{rLMMindepNormalGamma_reg}()},
\code{\link{simfuncs}},
\code{\link{two_block_d0_pilot_start}()},
\code{\link{two_block_l_for_tv}()},
\code{\link{two_block_m_convergence_for_pilot_start}()},
\code{\link{two_block_mode_weights}()},
\code{\link{two_block_optimize_pilot_cost}()},
\code{\link{two_block_pilot_sampling_cost}()},
\code{\link{two_block_rNormal_reg}()},
\code{\link{two_block_rNormal_reg_v2}()},
\code{\link{two_block_rate}()},
\code{\link{two_block_rate_v2}()},
\code{\link{two_block_tv_bound}()}
}
\concept{simfuncs}
