% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/envelopeorchestrator.R
\name{EnvelopeOrchestrator}
\alias{EnvelopeOrchestrator}
\title{Envelope Construction Orchestrator for Bayesian Gaussian Regression}
\usage{
EnvelopeOrchestrator(
  bstar2,
  A,
  y,
  x2,
  mu2,
  P2,
  alpha,
  wt,
  n,
  Gridtype,
  n_envopt,
  shape,
  rate,
  RSS_Post2,
  RSS_ML,
  max_disp_perc,
  disp_lower,
  disp_upper,
  use_parallel = TRUE,
  use_opencl = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{bstar2}{Numeric vector. Posterior mode of the standardized regression
coefficients (from the standardized model).}

\item{A}{Numeric matrix. Posterior precision matrix (Hessian) at the mode.}

\item{y}{Numeric response vector of length \code{m}.}

\item{x2}{Numeric matrix of standardized predictors (\code{m × p}).}

\item{mu2}{Numeric vector. Standardized prior mean (typically a zero vector).}

\item{P2}{Numeric matrix. Standardized prior precision component moved into
the log‑likelihood.}

\item{alpha}{Numeric vector. Offset‑adjusted mean component.}

\item{wt}{Numeric vector of prior weights.}

\item{n}{Integer. Number of envelope grid points or simulation draws.}

\item{Gridtype}{Integer specifying the envelope grid construction method.}

\item{n_envopt}{Optional integer. Effective sample size passed to
\code{EnvelopeOpt} during grid construction. Larger values encourage tighter
envelopes.}

\item{shape}{Numeric. Shape parameter of the Gamma prior for the dispersion.}

\item{rate}{Numeric. Rate parameter of the Gamma prior for the dispersion.}

\item{RSS_Post2}{Numeric. Posterior residual sum of squares used for
dispersion anchoring.}

\item{RSS_ML}{Numeric. Maximum‑likelihood residual sum of squares.}

\item{max_disp_perc}{Numeric in \code{(0,1)}. Tail probability used to
determine dispersion bounds when not explicitly supplied.}

\item{disp_lower}{Optional numeric. Lower bound for the dispersion
(\eqn{\sigma^2}). If supplied, overrides quantile‑based bounds.}

\item{disp_upper}{Optional numeric. Upper bound for the dispersion
(\eqn{\sigma^2}). Must be strictly greater than \code{disp_lower}.}

\item{use_parallel}{Logical. Whether to allow parallel computation inside
\link[glmbayes]{EnvelopeDispersionBuild}.}

\item{use_opencl}{Logical. Whether to allow OpenCL acceleration inside
\link[glmbayes]{EnvelopeBuild}.}

\item{verbose}{Logical. Whether to print detailed progress and timing
messages.}
}
\value{
A list with components:

\describe{
\item{\code{Env}}{The fully constructed and sorted envelope, including the
PLSD component inserted by the dispersion‑aware refinement step.}
\item{\code{gamma_list}}{Updated Gamma‑prior parameters for the dispersion
(shape, rate, and dispersion bounds).}
\item{\code{UB_list}}{Updated UB‑list including reordered
\code{lg_prob_factor} and \code{UB2min}.}
\item{\code{diagnostics}}{Diagnostic quantities returned by
\link[glmbayes]{EnvelopeDispersionBuild}, useful for debugging or envelope
visualization.}
\item{\code{low}}{Lower dispersion bound used.}
\item{\code{upp}}{Upper dispersion bound used.}
}
}
\description{
\code{EnvelopeOrchestrator()} provides a unified interface for constructing the
fixed‑dispersion and dispersion‑aware envelopes used in likelihood‑subgradient
simulation for Bayesian Gaussian regression with Normal–Gamma priors.

This function coordinates:
\itemize{
\item fixed‑dispersion envelope construction via \link[glmbayes]{EnvelopeBuild},
\item dispersion‑refined envelope construction via \link[glmbayes]{EnvelopeDispersionBuild},
\item envelope sorting and reindexing via \link[glmbayes]{EnvelopeSort}, and
\item UB‑list alignment (reordered \code{lg_prob_factor} and \code{UB2min}).
}

It is typically used inside *.cpp routines such as
\code{rIndepNormalGammaReg()}, but may also be called directly for
diagnostics, envelope visualization, or custom simulation workflows.
}
\details{
\code{EnvelopeOrchestrator()} consolidates the envelope‑related steps that were
previously distributed across multiple R and C++ routines.
It provides a stable, high‑level interface for envelope construction and
reduces the number of exported C++ functions required by the package.

The function does \strong{not} perform simulation.
Simulation should be carried out afterward using either
\code{.rindep_norm_gamma_reg_std_cpp()} or
\code{.rindep_norm_gamma_reg_std_parallel_cpp()}, depending on the
\code{use_parallel} flag.
}
\examples{
\dontrun{
env_out <- EnvelopeOrchestrator(
  bstar2 = bstar2,
  A = A,
  y = y,
  x2 = x2,
  mu2 = mu2,
  P2 = P2,
  alpha = alpha,
  wt = wt,
  n = 200,
  Gridtype = 2,
  n_envopt = NULL,
  shape = 2,
  rate = 1,
  RSS_Post2 = RSS_Post2,
  RSS_ML = RSS_ML,
  max_disp_perc = 0.99,
  disp_lower = NULL,
  disp_upper = NULL,
  use_parallel = TRUE,
  use_opencl = FALSE,
  verbose = TRUE
)

Env3 <- env_out$Env
}

}
\seealso{
\itemize{
\item \link[glmbayes]{EnvelopeBuild} – fixed‑dispersion envelope construction
\item \link[glmbayes]{EnvelopeDispersionBuild} – dispersion‑aware envelope refinement
\item \link[glmbayes]{EnvelopeSort} – envelope sorting and reindexing
\item \code{rIndepNormalGammaReg()} – full Normal–Gamma *.cpp simulation routine
}
}
