% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epsilon_optimize.R
\name{epsilon_optimize}
\alias{epsilon_optimize}
\title{Numerical epsilon(gamma*) from the minorization profile.}
\usage{
epsilon_optimize(
  mode,
  n = NULL,
  mc_seed = NULL,
  gamma_prime_start = NULL,
  control = list(maxit = 200L),
  ...
)
}
\arguments{
\item{mode}{Object returned by \code{\link{population_mode}}.}

\item{n}{Number of \eqn{\beta} draws for the MC inner integral when
the likelihood is not Gaussian. Defaults to \code{mode$n} from
\code{\link{population_mode}}, or \code{10000L}.}

\item{mc_seed}{Optional seed for the cached \eqn{\beta} draws (fixed for the
whole optimization).}

\item{gamma_prime_start}{Initial destination \eqn{\gamma'}; defaults to
\code{mode$gamma_star}.}

\item{control}{A list passed to \code{\link[stats]{optim}} (\code{method =
"BFGS"}).}

\item{...}{Ignored.}
}
\value{
A list with \code{eps_star}, \code{method}, \code{certified},
\code{g_opt}, \code{gamma_prime}, \code{optim}, and \code{n}.
}
\description{
Solves the convex program of Chapter C05 Definition 5 at the EM fixed
point: minimize \eqn{g(\gamma' \mid \gamma^\star) = \log q(\gamma' \mid
\gamma^\star) - \log q_Q(\gamma')} over destination \eqn{\gamma'}, with
the kernel integral over \eqn{\beta} uses an exact Gaussian convolution when
\code{family = gaussian()}, otherwise Monte Carlo draws from
\eqn{\pi(\beta \mid \gamma^\star, y)}.
}
\seealso{
\code{\link{population_mode}}, \code{\link{epsilon_star}}
}
