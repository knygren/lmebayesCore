% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/c05_certificate.R
\name{certificate}
\alias{certificate}
\title{Restricted two-block Gibbs minorization certificate (Chapter C05).}
\usage{
certificate(
  design,
  pfamily_list,
  family = gaussian(),
  delta = 0.01,
  dispprior_list = NULL,
  estep = c("exact", "aghq", "mc"),
  acceleration = c("none", "squarem"),
  icm_init = TRUE,
  icm_tol = 1e-08,
  icm_maxit = 200L,
  n = 10000L,
  mc_seed = NULL,
  tol = NULL,
  em_tol = 1e-10,
  maxit = 200L
)
}
\arguments{
\item{design}{A \code{\link{model_setup}} list.}

\item{pfamily_list}{Block~2 prior list from \code{\link{pfamily_list}()}.}

\item{family}{A \code{\link[stats]{family}} object.}

\item{delta}{Tail / escape probability budget in \code{(0, 1)}.}

\item{dispprior_list}{Optional Block~1 dispersion prior for \code{gaussian()}.}

\item{estep}{E-step tier: \code{"exact"} (default Gaussian closed form),
\code{"mc"} (simulated conditional means; valid for Gaussian and
non-Gaussian), or \code{"aghq"} (not yet implemented).}

\item{acceleration}{\code{"none"} or \code{"squarem"} (not yet implemented).}

\item{icm_init}{If \code{TRUE}, run iterated conditional modes (Block~1
modes via \code{\link[glmbayesCore]{rglmb}}, Block~2 via the C05 mean map)
before EM. Gaussian Block~1 uses exact conditional modes.}

\item{icm_tol}{ICM convergence tolerance on \eqn{\|\Delta\gamma\|_{P_{11}}}.}

\item{icm_maxit}{Maximum ICM iterations.}

\item{n}{Number of \eqn{\beta_j} draws per group when \code{estep = "mc"}.
The sample mean \eqn{\bar b_j} has elementwise MC standard error
\eqn{\mathrm{sd}(\beta_j \mid \gamma, y)/\sqrt{n}}; relative error on
means is therefore \eqn{O(1/\sqrt{n})} (e.g. \code{n = 10000} targets
\eqn{\approx 1\%} of the posterior scale).}

\item{mc_seed}{Optional seed for the MC E-step (first iteration only).}

\item{tol}{If supplied, compute a sweep count \code{n} such that
\code{(1 - eps)^n + delta <= tol} (Theorem 2).}

\item{em_tol}{EM convergence tolerance passed to \code{\link{population_mode}}
as \code{tol} (distinct from sweep-count \code{tol} below).}

\item{maxit}{Maximum EM iterations.}
}
\value{
An object of class \code{"minorization_certificate"}.
}
\description{
Computes the EM fixed point \eqn{\gamma^\star}, the mode profile
\eqn{\varepsilon(\gamma^\star)}, and the spectrum-calibrated restricted-chain
multiplier \eqn{\varepsilon = \varepsilon_d Q(\widetilde C_d)}.
}
\details{
The certificate applies to the \emph{restricted} Gibbs chain on
\eqn{\widetilde C_d}, not necessarily the production sampler; see
\code{inst/CHAPTER_C05_IMPLEMENTATION.md} section 8. Set sizing inverts
the weighted-\eqn{\chi^2} escape tail on the coupling spectrum at
\eqn{\gamma^\star} (section 4A.3). Outside Gaussian closure the exact
escape mass may differ from \code{delta}.
}
\seealso{
\code{\link{population_mode}},
\code{\link{epsilon}}, \code{\link{deficiency_spectrum}},
\code{\link{gamma_beta_tv_certificate}}
}
