% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rmerb_point.R
\name{rglmerb_point}
\alias{rglmerb_point}
\title{Point estimates for Bayesian GLMMs (\code{rglmerb(..., simulate = FALSE)})}
\usage{
rglmerb_point(
  design,
  pfamily_list,
  family = stats::poisson(),
  dispprior_list = NULL,
  offset = NULL,
  weights = 1,
  verbose = TRUE,
  print_icm_table = TRUE,
  tol = 1e-10,
  maxit = 200L,
  offset_missing = NULL,
  weights_missing = NULL
)
}
\arguments{
\item{design}{A \code{model_setup} object (from \code{\link{model_setup}}).}

\item{pfamily_list}{Named list of Block~2 \code{pfamily} objects, one per
random-effect coefficient in \code{design$groupef.names} (same as
\code{\link{rGLMM_reg}} / \code{\link{rLMMNormal_reg}}).}

\item{family}{A \code{\link[stats]{family}} object. Default \code{poisson()}.}

\item{dispprior_list}{Block~1 observation-dispersion prior, in the same
shapes as the \code{rLMM*} / \code{rGLMM*} engines. Required for
\code{family = gaussian()} (\code{list(dispersion = )} or
\code{dGamma()} / \code{dGamma_list()}); must be \code{NULL} (default)
for \code{poisson()} and \code{binomial()}.}

\item{offset, weights}{Observation offset and prior weights, as in
\code{\link{rGLMM_reg}} / \code{\link{rLMMNormal_reg}}
(\code{offset = NULL}, \code{weights = 1}). When omitted, inherit
\code{design$offset} / \code{design$weights} if present. Explicit values
always override the design. Echoed on the fit; not yet used in Gibbs
sweeps.}

\item{verbose}{Print stage headers and diagnostics.}

\item{tol, maxit}{ICM controls passed to \code{\link{glmerb_posterior_mode}}
(unused for the closed-form Gaussian mean).}

\item{offset_missing, weights_missing}{See \code{\link{rlmerb_point}}.}
}
\value{
An object of class \code{c("rglmerb", "list")} with mode fields,
full \code{prior}, thin \code{Prior}, and \code{NULL} draw slots.
}
\description{
Computes the joint posterior mode (ICM) at fixed variance-component
plug-ins, or the exact Gaussian mean when \code{family = gaussian()},
without MCMC draws. Called from \code{\link{rglmerb}} when
\code{simulate = FALSE}; not exported.
}
\seealso{
\code{\link{rglmerb}}, \code{\link{glmerb_posterior_mode}},
\code{\link{rlmerb_point}}
}
\keyword{internal}
