% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pp_check.glmb.R
\name{pp_check.glmb}
\alias{pp_check.glmb}
\title{Posterior predictive checks (\code{pp_check}) for \code{glmb} fits}
\usage{
\method{pp_check}{glmb}(object, ndraws = 100L, fun = "dens_overlay", ...)
}
\arguments{
\item{object}{A fitted model of class \code{glmb}.}

\item{ndraws}{Number of posterior draws to retain for \code{yrep} (first rows
of \code{predict(object, type = "response")}). The default is \code{100}.
Use \code{NULL} for all draws (can be slow or memory-heavy for large fits).}

\item{fun}{Passed to \code{\link[bayesplot:pp_check]{bayesplot::pp_check()}} as the \code{fun} argument: a
\verb{ppc_*} plotting function or its name without the \code{ppc_} prefix (see
\link[bayesplot:PPC-overview]{bayesplot::PPC-overview}).}

\item{...}{Additional arguments passed to \code{\link[bayesplot:pp_check]{bayesplot::pp_check()}}.}
}
\value{
A \pkg{ggplot2} object (from the \pkg{bayesplot} \code{ppc_*} function used).
}
\description{
Thin wrapper around \code{\link[bayesplot:pp_check]{bayesplot::pp_check()}} for objects of class \code{glmb}:
builds posterior predictive replications with \code{\link[=simulate.glmb]{simulate.glmb()}} and passes
\code{y} and \code{yrep} to the default \code{pp_check} method in \pkg{bayesplot}.
}
\details{
For binomial models with a two-column matrix response \code{cbind(successes,
failures)}, both \code{y} and \code{yrep} are expressed on the \strong{proportion}
scale so they match \code{\link[=simulate.glmb]{simulate.glmb()}}.

Call this function as \code{bayesplot::pp_check(object, ...)} (or attach
\pkg{bayesplot} first) so the S3 generic is visible.
}
\seealso{
\code{\link[=simulate.glmb]{simulate.glmb()}}, \code{\link[=predict.glmb]{predict.glmb()}}, \code{\link[bayesplot:pp_check]{bayesplot::pp_check()}}.
}
