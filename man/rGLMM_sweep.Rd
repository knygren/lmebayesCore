% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rGLMM_sweep.R
\name{rGLMM_sweep}
\alias{rGLMM_sweep}
\title{Two-block Gibbs sweep for replicate-chain sampling (\code{rGLMM_reg} engine)}
\usage{
rGLMM_sweep(
  n_chains,
  start_fixef,
  inner_sweeps,
  design,
  block1_prior,
  pfamily_list,
  family,
  re_names,
  group_levels,
  collect_block1 = TRUE,
  progbar = FALSE,
  stage_label = "",
  diag_sweeps = FALSE,
  fixef_mode = NULL,
  b_mode = NULL,
  b_start = NULL,
  ptypes = NULL,
  tau2_start = NULL,
  use_cpp_tau2_row = TRUE,
  use_cpp_b_slice = TRUE,
  use_cpp_iters_ranef_add = TRUE,
  use_cpp_block2 = TRUE
)
}
\arguments{
\item{n_chains}{Integer. Number of independent chains to run.}

\item{start_fixef}{Named list of starting hyper-parameter vectors, one
named numeric vector per RE component (matches \code{re_names}).}

\item{inner_sweeps}{Integer. Number of inner Gibbs sweeps per chain.}

\item{design}{List with components \code{y}, \code{Z}, \code{groups},
\code{X_hyper}, \code{re_coef_names}, and optional \code{group_name}.}

\item{block1_prior}{Block~1 prior list (\code{P} or \code{Sigma},
\code{dispersion}, optional \code{ddef}).}

\item{pfamily_list}{Named list of \code{\link[glmbayesCore]{pfamily}} objects (one per
RE component).}

\item{family}{A \code{\link[stats]{family}} object for the response model.}

\item{re_names}{Character vector of RE coefficient names.}

\item{group_levels}{Character vector of group levels.}

\item{collect_block1}{Logical. If \code{TRUE}, collect and rbind Block~1
(\code{coefficients}) draws from every chain. Default \code{TRUE}.}

\item{progbar}{Logical. When \code{TRUE}, show text progress bars over chains
during each inner sweep. Default \code{FALSE}.}

\item{stage_label}{Character label stored on \code{$sweep_history} (e.g.
\code{"pilot"} or \code{"main"}).}

\item{diag_sweeps}{When \code{TRUE}, print one combined Block~2 fixef
chain-mean table for the stage when the inner-sweep loop finishes (same
layout as \code{print()} on \code{$sweep_history} with all sweeps).
Sweep history is always collected; use \code{print()} on
\code{$sweep_history} to display tables later.}

\item{fixef_mode}{ICM mode reference stored on \code{$sweep_history}.}

\item{b_mode}{ICM mode reference for random-effect diagnostics.}

\item{b_start}{Initial random-effect matrix for all chains (\code{J x p_re}).
Defaults to \code{b_mode} when \code{NULL}.}

\item{ptypes}{Per-component pfamily names (optional; derived from
\code{pfamily_list} when \code{NULL}).}

\item{tau2_start}{Optional named numeric vector of plug-in
\eqn{\tau^2_k} values for chain initialisation (one per \code{re_names}).
When \code{NULL}, derived from \code{pfamily_list} prior fields
(\code{dNormal} dispersion or ING \code{rate/shape} = \eqn{1/E[1/\tau^2]}).}

\item{use_cpp_tau2_row}{Block~1 step A (\code{batch$tau2[i, ]}): when \code{TRUE}
(default), use \code{.two_block_batch_tau2_chain_row_cpp}; \code{FALSE}
uses pure R row extract (reference oracle during migration).}

\item{use_cpp_b_slice}{Block~1 step C (\code{batch$b[, , i] <- out$b}): when
\code{TRUE} (default), use \code{.two_block_batch_b_assign_slice_cpp};
\code{FALSE} uses pure R subassignment.}

\item{use_cpp_iters_ranef_add}{Block~1 step D
(\code{batch$iters_ranef[i] += out$iters_mean}): when \code{TRUE} (default),
use \code{.two_block_batch_iters_ranef_add_cpp}; \code{FALSE} uses pure R
accumulation.}

\item{use_cpp_block2}{When \code{TRUE}, Block~2 uses
\code{\link{two_block_block2_one_chain_cpp}} (native C++ align + \code{rglmb})
instead of the pure-R reference.}
}
\value{
A list with components \code{fixef_draws}, \code{dispersion_fixef_draws},
\code{iters_fixef_draws}, \code{iters_ranef_draws}, \code{coefficients},
\code{mu_all_last}, and \code{sweep_history} (class
\code{"two_block_sweep_history"}).
}
\description{
Executes \code{n_chains} independent two-block Gibbs chains, each
initialised at \code{start_fixef} and run for \code{inner_sweeps} sweeps.
Loop order is sweep-outer: for each inner sweep, all chains run Block~1,
then all chains run Block~2. Each chain stores exactly one draw
(\code{n = 1L} per chain), so the result is a collection of
\code{n_chains} approximately independent draws from the target posterior
(for large enough \code{inner_sweeps}).
}
\seealso{
\code{\link{two_block_rNormal_reg}}, \code{\link{rGLMM_reg}}

glmbayes Simulation Functions
\code{\link{rGLMM_Re_Draw}()},
\code{\link{rGLMM_reg}()},
\code{\link{rLMMNormal_joint_iid}()},
\code{\link{rLMM_reg}},
\code{\link{two_block_optimize_pilot_cost}()},
\code{\link{two_block_rNormal_reg}()},
\code{\link{two_block_rate}()},
\code{\link{two_block_tv_bound}()}
}
\concept{simfuncs}
