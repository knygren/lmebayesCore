% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rglmerb.R
\name{rglmerb}
\alias{rglmerb}
\title{The Bayesian Generalized Linear Mixed-Effects Model Distribution}
\usage{
rglmerb(
  n,
  design,
  prior,
  family = poisson(),
  dispersion_ranef = NULL,
  fixef_start = NULL,
  m_convergence = NULL,
  gap_tol = 0.0196,
  tv_tol = 0.01,
  mode_gap_max = 1,
  collect_block1 = TRUE,
  verbose = TRUE,
  progbar = FALSE
)
}
\arguments{
\item{n}{Integer. Number of independent chains in the main stage.}

\item{design}{A \code{model_setup} object (from \code{\link{model_setup}}).}

\item{prior}{A normalized prior container with \code{Sigma_ranef},
\code{prior_list}, and \code{pfamily_list}.}

\item{family}{A \code{\link[stats]{family}} object. Default \code{poisson()}.}

\item{dispersion_ranef}{Observation-level measurement dispersion \eqn{\sigma^2}:
required positive scalar for \code{family = gaussian()}, or a
\code{\link{dGamma}()} pfamily with \code{Inv_Dispersion = TRUE}; must be
\code{NULL} (default) for \code{poisson()} and \code{binomial()}.}

\item{fixef_start}{Optional named list of Block~2 starting vectors. When
\code{NULL}, the ICM start is computed inside the Core engine
(\code{\link{lmerb_posterior_mean}} for Gaussian,
\code{\link{glmerb_posterior_mode}} otherwise).}

\item{m_convergence}{Optional integer inner Gibbs sweeps per main draw.}

\item{gap_tol}{Legacy mode--mean gap for deriving the pilot chain count when
\code{tv_tol} is \code{NULL}. Ignored for Gaussian without ING Block~2
components.}

\item{tv_tol}{Total variation tolerance for convergence calibration.}

\item{mode_gap_max}{Pilot inner-sweep calibration (non-Gaussian and
Gaussian+ING only).}

\item{collect_block1}{Collect Block~1 \code{coefficients} from main chains
(non-Gaussian only).}

\item{verbose}{Print stage headers and diagnostics.}

\item{progbar}{Progress bars when \code{verbose} is \code{FALSE}.}
}
\value{
Object of class \code{c("rglmerb", "list")} with Block~2 fields in
the \code{fixef.*} namespace, plus \code{ranef.mode}, \code{Prior},
\code{design}, and \code{family}. Non-Gaussian fits may include
\code{n_pilot}, \code{pilot}, and \code{pilot_chisq}; Gaussian fits set
\code{n_pilot = 0L} and omit pilot output.
}
\description{
Bayesian generalized linear mixed-effects model sampler
}
\details{
Matrix-level sampler for \code{model_setup} design objects and normalized
prior containers. Routes by response family:
\itemize{
\item \code{family = gaussian()} delegates to \code{\link{rLMMNormal_reg}} or
\code{\link{rLMMindepNormalGamma_reg}} when \code{dispersion_ranef} is a
\code{\link{dGamma}()} pfamily.
\item Non-Gaussian families delegate to \code{\link{rGLMM}}
(\code{\link{rGLMM_sweep}} sweep-outer driver).
}

For formula-level fitting, \code{glmerb()} in the lmebayes package wraps this sampler.
Design matrices are built with \code{\link{model_setup}} and priors with
\code{\link{Prior_Setup_lmebayes}}.
}
\seealso{
\code{\link{rlmerb}}, \code{\link{rLMMNormal_reg}},
\code{\link{rLMMindepNormalGamma_reg}}, \code{\link{rGLMM}},
\code{\link{Prior_Setup_lmebayes}}
}
