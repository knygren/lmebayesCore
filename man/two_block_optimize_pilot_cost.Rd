% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two_block_pilot_cost.R
\name{two_block_optimize_pilot_cost}
\alias{two_block_optimize_pilot_cost}
\alias{two_block_pilot_sampling_cost}
\alias{two_block_m_convergence_for_pilot_start}
\alias{two_block_d0_pilot_start}
\title{Pilot / main chain cost calibration for two-block GLMM sampling}
\usage{
two_block_d0_pilot_start(n_pilot, p, pilot_start_tol = 0.95)

two_block_m_convergence_for_pilot_start(
  rate,
  n_pilot,
  tv_tol,
  p,
  pilot_start_tol = 0.95,
  method = c("theorem3", "corollary1")
)

two_block_pilot_sampling_cost(
  n,
  n_pilot,
  rate,
  tv_tol,
  m_convergence_pilot,
  p,
  pilot_start_tol = 0.95,
  method = c("theorem3", "corollary1")
)

two_block_optimize_pilot_cost(
  n,
  rate,
  tv_tol,
  m_convergence_pilot,
  p,
  pilot_start_tol = 0.95,
  method = c("theorem3", "corollary1"),
  n_pilot_min = 1L,
  n_pilot_max = NULL
)
}
\arguments{
\item{n_pilot}{Number of pilot replicate chains (positive integer).}

\item{p}{Dimension of the Block~2 hyper vector (positive integer).}

\item{pilot_start_tol}{One-sided chi-squared quantile in \code{(0, 1)}
for the \eqn{D_0} certificate (default \code{0.95}).}

\item{rate}{Object from \code{\link{two_block_rate}}.}

\item{tv_tol}{Target total-variation tolerance in \code{(0, 1)} passed to
\code{\link{two_block_l_for_tv}} when calibrating \code{m_convergence}.}

\item{method}{\code{"theorem3"} (exact terms) or \code{"corollary1"}
(geometric envelope).}

\item{n}{Number of main-stage stored draws.}

\item{m_convergence_pilot}{Inner Gibbs sweeps per pilot stored draw
(\eqn{m_p}).}

\item{n_pilot_min, n_pilot_max}{Integer bounds for the \code{n_pilot} search
in \code{two_block_optimize_pilot_cost} (inclusive).  When
\code{n_pilot_max} is \code{NULL}, defaults to \code{max(10000L, 10L * n)}.}
}
\value{
Non-negative scalar \eqn{D_0}.

List with \code{m_convergence} (integer sweeps per main draw),
\code{m_min} (mode-start minimum), \code{delta_m}, \code{D_0}, and
\code{n_pilot}.

List with \code{total_cost}, \code{pilot_cost}, \code{main_cost},
\code{m_convergence}, and the fields returned by
\code{two_block_m_convergence_for_pilot_start}.

List with \code{n_pilot_opt}, \code{m_convergence_opt},
\code{total_cost_opt}, \code{cost_at_opt}, and \code{cost_curve} (data
frame of evaluated \code{n_pilot} values near the optimum).
}
\description{
Tools for the pilot-vs-main trade-off in non-Gaussian two-block GLMM paths:
certificate the start distance when main chains begin at a pilot mean,
calibrate \code{m_convergence} from Nygren TV bounds, evaluate total
inner-sweep cost, and search for a cost-optimal \code{n_pilot}.
}
\details{
The functions form a bottom-up stack:
\enumerate{
\item \code{\link{two_block_d0_pilot_start}}:
\eqn{D_0 = \chi^2_{p,\alpha} / n_{\mathrm{pilot}}} for a pilot-mean start.
\item \code{\link{two_block_m_convergence_for_pilot_start}}: main-stage
\code{m_convergence} at that \eqn{D_0} (via \code{\link{two_block_l_for_tv}}).
\item \code{\link{two_block_pilot_sampling_cost}}:
\eqn{C = n_{\mathrm{pilot}} m_p + n\, m_{\mathrm{conv}}(n_{\mathrm{pilot}})}.
\item \code{\link{two_block_optimize_pilot_cost}}: minimize \eqn{C} over
\code{n_pilot} (advisory unless wired into \code{\link{rGLMM_reg}}).
}
This is the Gibbs analogue of \code{\link[glmbayesCore]{EnvelopeOpt}}'s build-plus-sample cost.
}
\section{Functions}{
\itemize{
\item \code{two_block_d0_pilot_start()}: Squared standardized start distance
from a pilot mean start: \eqn{D_0 = \chi^2_{p,\alpha} / n_{\mathrm{pilot}}}.

\item \code{two_block_m_convergence_for_pilot_start()}: Main-stage inner sweeps for a
pilot-mean chain start.  When \code{n_pilot} is large, \code{D_0} is small
and the result approaches the mode-start minimum \code{m_min}.

\item \code{two_block_pilot_sampling_cost()}: Total inner-sweep cost for a
pilot / main sampling plan:
\eqn{C = n_{\mathrm{pilot}} m_p + n\, m_{\mathrm{conv}}(n_{\mathrm{pilot}})}.

\item \code{two_block_optimize_pilot_cost()}: Search over candidate
\code{n_pilot} values for the minimum of
\code{\link{two_block_pilot_sampling_cost}}.  The returned optimum is
advisory only unless wired into the sampler.

}}
\seealso{
\code{\link{two_block_rate}}, \code{\link{two_block_l_for_tv}},
\code{\link[glmbayesCore]{EnvelopeOpt}}, \code{\link{rGLMM_reg}}

glmbayes Simulation Functions
\code{\link{rGLMM_Re_Draw}()},
\code{\link{rGLMM_reg}()},
\code{\link{rGLMM_sweep}()},
\code{\link{rLMMNormal_joint_iid}()},
\code{\link{rLMM_reg}},
\code{\link{two_block_rNormal_reg}()},
\code{\link{two_block_rate}()},
\code{\link{two_block_tv_bound}()}
}
\concept{simfuncs}
